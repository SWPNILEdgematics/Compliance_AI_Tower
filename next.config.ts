import type { NextConfig } from "next";

const nextConfig = {
  // Your existing config
  env: {
    // You don't need to add NEXT_PUBLIC_ vars here, they're automatically exposed
  },
} as NextConfig;

export default nextConfig;
